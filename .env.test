# =============================================================================
# TEST ENVIRONMENT CONFIGURATION
# Uses local Docker PostgreSQL, paper trading APIs
# =============================================================================

# Alpaca API Configuration (Real market data)
ALPACA_API_KEY=${ALPACA_API_KEY}
ALPACA_SECRET_KEY=${ALPACA_SECRET_KEY}
ALPACA_BASE_URL=https://api.alpaca.markets

# Application Configuration
APP_ENV=test
LOG_LEVEL=DEBUG

# Claude API (uses real API for SDK testing)
CLAUDE_API_KEY=${CLAUDE_API_KEY}
CLAUDE_MODEL_PLANNING=claude-sonnet-4-20250514
CLAUDE_MODEL_FAST=claude-haiku-4-5-20251001

# Local PostgreSQL (from docker-compose, port 5433 to avoid conflicts)
DATABASE_URL=postgresql://stockmate:localdev@localhost:5433/stockmate

# Supabase - DISABLED for local testing
# Auth will be bypassed in test mode
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_KEY=
SUPABASE_JWT_SECRET=test-jwt-secret-for-local-development-only

# Rate Limiting (relaxed for testing)
RATE_LIMIT_PER_MINUTE=1000
RATE_LIMIT_AI_PER_MINUTE=100

# Agent Configuration
AGENT_ANALYSIS_INTERVAL=60
ALERT_COOLDOWN_MINUTES=1
MAX_CONCURRENT_AGENTS=5

# Test Mode Flag
TEST_MODE=true
BYPASS_AUTH=true
